---
title: Validable Protocol
description: Objects that can be validated
---

The `Validable` protocol represents objects that can be validated.

---

## Overview

```python
from better_py.protocols import Validable

class Email(Validable):
    def validate(self):
        if "@" in self.value:
            return Result.ok(self)
        return Result.error("Invalid email")
```

---

## See Also

- [Parseable](/docs/protocols/parseable) - Parse protocol
- [Protocols Overview](/docs/protocols)
