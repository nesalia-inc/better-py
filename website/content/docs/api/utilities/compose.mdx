---
title: compose() API
description: compose function reference
---

# compose()

Right-to-left function composition.

## Signature

```python
def compose(*functions: Callable) -> Callable:
    ...
```

## Example

```python
pipeline = compose(str, lambda x: x * 2)
result = pipeline(5)  # "10"
```

## See Also

- [compose Guide](/docs/utilities/compose)
